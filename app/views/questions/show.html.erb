<p id="notice"><%= notice %></p>
<div class="card shadow">
	<div class="card-header">
  		<%= @question.title %>
  </div>
  <div class="card-body">
  		<%= @question.description %>
	</div>
</div>
<%= link_to 'Edit', edit_question_path(@question), class: 'btn btn-primary mt-3' %> 
<%= link_to 'Back', questions_path , class: 'btn btn-primary mt-3' %>
